
#include "FaceRecognise.h"


int CFaceRecognise::DetectFace(string& filePath, FACES_LOCACTIONS faces)
{
	int nFaces = 0;		// the numbers of detected faces

	return nFaces;
}

int CFaceRecognise::DetectFace(Mat& image, FACES_LOCACTIONS faces)
{
	int nFaces = 0;		// the numbers of detected faces
	return nFaces;
}

int CFaceRecognise::GetFace(string& filePath, FACE_INFO& fi)
{
	int nRet = 0;

	return nRet;
}

int CFaceRecognise::GetFace(Mat& image, FACE_INFO& fi)
{
	int nRet = 0;

	return nRet;
}

